# Configurações do scraper. Copie para .env e ajuste se necessário.

# Browser
HEADLESS=true
WAIT_MS=3000
TIMEOUT_MS=30000

# Persistência (caminho do SQLite local)
DB_PATH=./price_results.db

# Comportamento
RESOLVE_STORE_URL=true
MAX_RESULTS_PER_ITEM=5
